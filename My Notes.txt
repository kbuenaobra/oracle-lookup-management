Installed VS Code, Oracle Express 21c
Sql Developer Extension

asked agent to run lookup.md 
-- a lot of asking, checking done

it created a test db path first
-- asked ai to use my oracle database 21c instead


streamlit run app.py
http://localhost:8502/

To save to repository:
============================
git init
git add .
git commit -am "Apr20Commit_1"
--git remote add origin https://github.com/kbuenaobra/oracle-lookup-management.git
git push origin master

To run project:
============================
Set-Location "c:\Users\user\Documents\Working Folder\AI\Oracle Lookup Project"
& ".\venv\Scripts\python.exe" create_schema.py
& ".\venv\Scripts\python.exe" -m streamlit run app.py

If you want the easiest path, just double-click:
[start_project.bat](c:/Users/user/Documents/Working%20Folder/AI/Oracle Lookup Project/start_project.bat)

If the app is already running on port 8502, stop that instance first or start a new one on another port:
& ".\venv\Scripts\python.exe" -m streamlit run app.py --server.port 8504
